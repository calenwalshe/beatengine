# Pending Tasks

- [ ] Build & upload release tarball (make dist) + checksums and publish GitHub Release.
- [ ] Optional YAML loader support alongside JSON EngineConfig.
- [ ] Optional CI job to run `scripts/bootstrap.sh` on macOS runner.
- [ ] Review roadmap checklist docs items and mark complete once external documentation is finalized.
